toolsets = vs2017;

vs2017.solutionfile = ../VC15/DiplodocusEmbeddedTreeDB.sln;

library DiplodocusEmbeddedSecretsDB
{
    archs = x86 x86_64;
   
    includedirs += ../../../Include/DiplodocusDB/SecretsDB/EmbeddedSecretsDB;

    if ($(toolset) == gnu)
    {
        cxx-compiler-options = "-std=c++11";
        outputdir = ../../../Lib;
    }

    if ($(toolset) != gnu)
    {
        outputdir = ../../../Lib/$(arch);
    }

    if ($(config) == Debug)
    {
        basename = DiplodocusEmbeddedSecretsDB-d;
    }

    headers
    {
        ../../../Include/DiplodocusDB/SecretsDB/EmbeddedSecretsDB.h
    }

    sources
    {
        ../../Source/EmbeddedSecretsDB.cpp
    }
}
